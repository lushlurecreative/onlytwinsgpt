# Face-swap worker - GPU path
# onnxruntime-gpu installed separately in Dockerfile (needs CUDA 12 index)

# HTTP server
flask==3.0.0
requests==2.31.0

# Storage
python-dotenv==1.0.0

# Face swap (FaceFusion library - industry leading quality)
facefusionlib>=1.1.3

# Image processing
opencv-python-headless==4.8.1.78
numpy==1.24.3

# Required by facefusionlib
filetype==1.2.0
onnx==1.15.0
tqdm
