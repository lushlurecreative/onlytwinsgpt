# Minimal face-swap worker - GPU path
# Pinned versions for reproducibility
# GPU inference only - no training, no FLUX

# HTTP server
flask==3.0.0
requests==2.31.0

# Storage (upload uses direct REST via requests — no supabase SDK needed)
python-dotenv==1.0.0

# Face swap core
insightface==0.7.3
onnxruntime-gpu==1.17.1

# Image processing
opencv-python-headless==4.8.1.78
numpy==1.24.3
