# Face-swap worker - GPU path (RunPod Serverless)
# onnxruntime-gpu installed separately in Dockerfile (needs CUDA 12 index)

# RunPod Serverless SDK
runpod>=1.6.0

# HTTP client (for downloading images)
requests==2.31.0

# Storage
python-dotenv==1.0.0

# Face swap (FaceFusion library - industry leading quality)
facefusionlib>=1.1.3

# Image processing
opencv-python-headless==4.8.1.78
numpy==1.24.3

# Required by facefusionlib
filetype==1.2.0
onnx==1.15.0
tqdm
