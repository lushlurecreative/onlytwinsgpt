# ONLYTWINS RunPod worker – copy to .env and set values.
# WORKER_SECRET must match the app (Vercel env). Generate: openssl rand -hex 32

WORKER_SECRET=
APP_URL=https://your-app.vercel.app
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Optional: direct Postgres instead of app internal API
# DATABASE_URL=postgresql://...

# Optional: poll interval in seconds (default 15)
# WORKER_POLL_INTERVAL_SEC=15
