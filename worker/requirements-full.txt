# ONLYTWINS RunPod worker – full stack (FLUX LoRA training + generation + Real-ESRGAN)
# Install (GPU): pip install torch torchvision then pip install -r requirements-full.txt
# Requires: Python 3.10+, CUDA, ~24GB+ VRAM for training, ~12GB+ for inference

runpod>=1.0.0
requests>=2.28.0
python-dotenv>=1.0.0
supabase>=2.0.0
psycopg2-binary>=2.9.0

# ML stack
torch>=2.0.0
torchvision
diffusers>=0.36.0
transformers>=4.41.0
accelerate>=0.31.0
peft>=0.11.0
safetensors
sentencepiece

# Optional: 4-bit training (saves VRAM)
# bitsandbytes

# Watermark (forensic embed/decode)
invisible-watermark>=0.2.0
opencv-python-headless>=4.8.0

# Upscale (optional; generation still works without it)
# realesrgan  # or: pip install git+https://github.com/sberbank-ai/Real-ESRGAN.git
