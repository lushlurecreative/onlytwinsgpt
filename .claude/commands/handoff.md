Finish the current session and update docs only. No app code changes.

Tasks:
1. Update `docs/HANDOFF_MASTER.md` with a compact, accurate handoff.
2. Update the single relevant bug file in `docs/bugs/` for the issue worked on in this session.
3. Do not create new files outside this command setup.
4. Do not add fluff or guesses.
5. Replace stale claims instead of appending endless history.

For `docs/HANDOFF_MASTER.md`, keep only:
- Project goal
- Current status
- Active bugs
- Changes made recently
- Known facts
- Open hypotheses
- Next best single step
- Do not repeat

For the active bug file, update only:
- Expected behavior (if needed)
- Actual behavior (if changed)
- Confirmed facts
- Things already tried
- Next single step
- Status if materially changed

At the end, output:
1. files updated
2. exact sections changed
3. anything still unknown
4. recommended name of the next session objective
