# Architecture

## Overview

Next.js 16 App Router monolith deployed on Vercel. All business logic lives in `lib/`. Routes live in `app/`. The database is Supabase Postgres. Stripe handles all payments. RunPod handles all AI jobs.

See `docs/architecture.md` for the detailed architecture reference.
